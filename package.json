{
  "name": "sightline",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create sightline",
    "db:local": "wrangler d1 migrations apply sightline --local",
    "db:remote": "wrangler d1 migrations apply sightline --remote",
    "db:console": "wrangler d1 execute sightline --local --command"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.10.0"
  }
}
